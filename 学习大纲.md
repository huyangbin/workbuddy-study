# WorkBuddy 学习大纲

> 适用对象：有一定基础的进阶学习者  
> 目标：系统掌握 WorkBuddy 的能力边界、运行机制与实战用法，能把工具用出生产力  
> 学习节奏建议：每个模块"看懂 → 模仿 → 独立实操"三步走，配套练习务必动手  
> 💡 本文档每个模块均为**可折叠**区块，点击标题即可展开/收起，方便随时聚焦当前进度。

---

<details open>
<summary><b>总览：WorkBuddy 是什么</b></summary>

WorkBuddy 是一个**以工具调用驱动**的 AI 助手，不是"聊天机器人"。它的核心差异点：

- **能动手**：读写文件、跑命令、生成图片/视频/3D、操作浏览器
- **有记忆**：跨会话的云端画像 + 本地/项目记忆
- **可扩展**：Skills（技能）、Connectors（连接器）、Experts（专家）、MCP 四类扩展机制
- **能自动化**：一次性与周期性的定时任务
- **能交付**：把结果打包成网页、文档、应用、可执行产物

学习它的关键心法是：**不要把它当搜索引擎，要当成一个拥有全套开发/创作环境的"实习生"——你下指令，它调用工具去执行。**

</details>

---

<details open>
<summary><b>学习路径总图</b></summary>

```
L0 认知      →  了解能力边界与交互模式
L1 基础操作  →  文件、命令、对话、上下文
L2 工具系统  →  核心工具分类与调用规则
L3 技能系统  →  Skills 的发现、使用、创建
L4 自动化    →  Automations 定时与周期任务
L5 扩展生态  →  Connectors / Experts / MCP
L6 多模态    →  图片 / 视频 / 3D 生成
L7 可视化    →  Visualizer 内联图表与图示
L8 记忆系统  →  三层记忆的读写策略
L9 实战项目  →  端到端交付一个真实项目
L10 进阶交付 →  发布应用 / 资料库 / 打包
```

</details>

---

<details>
<summary><b>L0 · 认知与定位（0.5 天）</b></summary>

**目标**：建立"WorkBuddy 不是聊天而是工具执行体"的心智模型。

- [ ] 理解 agent loop：分析 → 思考 → 选工具 → 执行 → 观察 → 迭代
- [ ] 三种工作模式：Agent（直接干）/ Plan（先规划）/ Ask（只说不动）
- [ ] 结果呈现机制：`present_files` 是每次任务结束的"交付入口"
- [ ] 中文环境与区域约定（红涨绿跌、¥ 货币、地图合规）
- [ ] 阅读官方文档入口：<https://www.workbuddy.cn/docs/workbuddy/Overview>

**练习**：用一句话描述 WorkBuddy 与你用过的其他 AI 工具（如纯对话模型）的本质区别。

</details>

---

<details>
<summary><b>L1 · 基础操作（1 天）</b></summary>

**目标**：掌握文件、命令、对话三件套。

- [ ] 文件读写：`Read` / `Write` / `Edit` 的精确使用（Edit 必须 old/new 精确匹配）
- [ ] 文件搜索：`Glob`（按名）与 `Grep`（按内容）的正确分工
- [ ] 命令执行：`Bash` / `PowerShell` 的沙箱与权限边界
- [ ] 运行环境：managed Python 3.13 / Node 22 的隔离规则（venv、NODE_PATH）
- [ ] 截图反馈循环：用截图暴露 UI/视觉问题 → 即时定位根因

> **运行环境详解（实测补充）**
>
> **路径（本机真实确认）**
>
> - managed Python 3.13.12（首选）：`C:\Users\27598\.workbuddy\binaries\python\versions\3.13.12\python.exe`
> - managed Python 3.12.4（系统回退）：`C:\Users\27598\miniconda3\python.exe`
> - managed Node 22.22.2（首选）：`C:\Users\27598\.workbuddy\binaries\node\versions\22.22.2-2\node.exe`
> - Node 24.13.0（系统回退）：`C:\Program Files\nodejs\node.exe`
>
> 调用时**务必用完整绝对路径**，不要裸写 `python`/`node`，否则会落到系统解释器。
>
> **它算虚拟环境吗？——不算，是"隔离目录里的独立便携解释器"。**
>
> - 传统 `venv` 是轻量副本，依赖一个已存在的 base Python 的 stdlib，只隔离 site-packages。
> - managed 运行时是**完整自带的 Python/Node**，整体装在 WorkBuddy 自家目录 `C:\Users\27598\.workbuddy\binaries\` 内，与系统解释器完全切断——WorkBuddy 可随意升级/替换而不碰系统。隔离发生在**目录级**，不是 venv 级。
> - 装包约定：在它下面再建 venv 来隔离第三方库，避免污染 base：  
>   `C:\Users\27598\.workbuddy\binaries\python\versions\3.13.12\python.exe -m venv C:\Users\27598\.workbuddy\binaries\python\envs\default`
> - 实测：当前 `envs/` 目录尚不存在（未建过 venv），base 运行时里除 `pip 26.1.2` 外无任何第三方库。
>
> **怎么看已安装的 Python 库？**
>
> - 看 base 运行时：`C:\Users\27598\.workbuddy\binaries\python\versions\3.13.12\python.exe -m pip list`
> - 建 venv 后改用 venv 的 pip（Windows 为 `Scripts\pip.exe`）：`C:\Users\27598\.workbuddy\binaries\python\envs\default\Scripts\pip.exe list`
> - 或直接列目录 `Lib\site-packages\`。
> - 实测 `pip list` 仅输出 `pip 26.1.2`，`Lib\site-packages\` 下只有 `pip` 与 `README.txt`。

**练习**：在 `项目-学习1` 下新建 `practice/` 文件夹，写一段脚本并用 Bash 执行，把输出读回来。（已完成：见 `practice/scan_project.py`）

</details>

---

<details>
<summary><b>L2 · 工具系统（1.5 天）</b></summary>

**目标**：理解 WorkBuddy 可调用的核心工具族，知道"什么任务用什么工具"。

<details>
<summary>2.1 文件系统类</summary>

`Read` `Write` `Edit` `Glob` `Grep` `Bash` `PowerShell`

</details>

<details>
<summary>2.2 智能体编排类</summary>

`Agent`（子代理：general / Explore / Plan / sheet-agent 等）、`TaskCreate/Update/List`（任务管理）

</details>

<details>
<summary>2.3 任务与自动化类</summary>

`automation_update`（一次性/周期任务）、`TaskOutput`（后台任务回收）

</details>

<details>
<summary>2.4 研究与联网类</summary>

`WebSearch` `WebFetch`（注意敏感/合规边界）

</details>

<details>
<summary>2.5 可视化类</summary>

`read_me` `show_widget`（内联 SVG/HTML 图表）

</details>

<details>
<summary>2.6 交付与扩展类</summary>

`present_files`、`Skill`、`ToolSearch`/`DeferExecuteTool`（延迟工具）

</details>

**练习**：画一张"任务类型 → 推荐工具"的对照表，作为自己的速查卡。（已完成：见 `practice/工具速查卡.md`）

</details>

---

<details>
<summary><b>L3 · 技能系统 Skills（2 天，重点）</b></summary>

**目标**：会用、会找、会造技能。这是 WorkBuddy 生产力放大的核心。

- [ ] 技能的分级：User 级（`~/.workbuddy/skills/`）vs Project 级（`项目/.workbuddy/skills/`）
- [ ] 调用技能：`Skill` 工具，匹配即优先加载
- [ ] 发现技能：`find-skills` / marketplace 安装（推荐市场 BuiltinMarket）
- [ ] 创建技能：`skill-creator` 引导，沉淀可复用工作流
- [ ] 安全审计：安装第三方技能前必须走 `skills-security-check`
- [ ] 迭代改进：用后发现过时/错误，立即 `SkillManage` 修正

**重点技能清单（建议逐个实操）**：

> ✅ 已实操：`tencent-pptx` —— 已生成 `practice/WorkBuddy速览/WorkBuddy速览.pptx`（3 页：封面 / 核心能力 / 工作模式）。流程：加载技能 → 写 `STORY.md`+`DESIGN.md` → `slidep create` 注册 → 写 `.slide` DSL → `slidep-validate` → `slidep upsert-dsl`。**踩坑**：本环境 `FAIcon` 图标资源未部署，必须改用内联 `<svg>` 线形图标；`slidep` CLI 在 managed Node 版本目录 `...\node\versions\22.22.2-2\` 下（带 `.cmd` 包装），需加该目录到 PATH。

- `tencent-docx` / `tencent-pptx` / `tencent-docs-sheet-generation`（Office 全家桶）
- `tencent-docs-routing`（本地 Office 路由入口）
- ✅ 已实操：`pdf-convert`（自造用户级技能，路径 `~/.workbuddy/skills/pdf-convert/`）—— 统一脚本 `pdf_convert.py` 含 8 个子命令 `pdf2img` `img2pdf` `pdf2excel` `excel2pdf` `html2pdf` `pdf2html` `pdf2docx` `pdf2pptx`，覆盖 PDF 与 图片 / Excel / HTML / Word / PPT 的全格式互转，全部实测通过（产物校验 ALL VALID）。**关键结论（沙箱实测）**：LibreOffice 在沙箱里**只能写 PDF、写不出合法 OOXML（docx/pptx/xlsx 导出必为空壳）**，故"写出 Office"一律走纯 Python（docx 手写 OOXML / pptx 用 python-pptx / xlsx 用 openpyxl），"Office/HTML→PDF"才用 LibreOffice 优先 + reportlab 兜底（Calc→PDF 可用，与 Impress 不同）。依赖收口在 `.venv-pdf`（pypdfium2/pdfplumber/reportlab/pypdf/Pillow/openpyxl/python-pptx）。
- `pdf` / `pdfkit-py`（PDF 处理参考技能）
- `agent-browser` / `playwright-cli`（浏览器自动化）
- `geo-map-compliance-guard`（地图合规，做地理项目必看）
- `expert-manager`（专家包管理）
- `发布为应用` / `资料库`（交付与协作）

**练习**：把你 L1 写的那段脚本封装成一个 Project 级技能，下次能一句话复用。

> 📌 **已落地示例**：上方"PDF 全格式互转"需求已封装为 `pdf-convert` 用户级技能（路径 `~/.workbuddy/skills/pdf-convert/`），以后再提"PDF 转 Word/PPT/Excel"等需求可一句话触发，无需重复踩沙箱坑。这正好印证了"会造技能 = 生产力放大器"。

</details>

---

<details>
<summary><b>L4 · 自动化 Automations（1 天）</b></summary>

**目标**：让 WorkBuddy 在指定时间/周期自动干活。

- [ ] 一次性任务：`scheduleType=once` + `scheduledAt`（ISO8601）
- [ ] 周期任务：`rrule` 写法（DAILY/WEEKLY/MONTHLY/YEARLY 的必填字段）
- [ ] 提示词自包含原则：未来运行看不到本次对话，细节必须写进 prompt
- [ ] 三个关键字段（务必理解）：
  - `status`：**运行状态**。`ACTIVE` 到时间真会跑；`PAUSED` 已创建但暂停、不触发。搭框架/调试时先设 `PAUSED`，确认无误再切 `ACTIVE`。
  - `validFrom` / `validUntil`：调度的**生效时间窗口**（ISO 8601 日期或日期时间，如 `2026-09-15` 或 `2026-09-15T09:00`），不填 = 无限期。`validFrom` 之前不触发，`validUntil` 之后自动失效。
  - `cwds`：**工作目录**（comma-separated，可多个）。自动化运行时的"立足目录"，决定相对路径怎么算、文件落盘到哪、能否读到项目素材。务必指向目标项目目录。
- [ ] 反模式：不要用自动化覆盖"现在就要做"的一次性请求

**练习**：创建一个"每周一 9:00 自动汇总上周项目进展"的周期性自动化（先不真正启用，写出正确的 rrule）。

> 📌 **已落地示例**：已创建一个 PAUSED 状态的周期性自动化「GitHub 热门项目周报」——`rrule=FREQ=WEEKLY;BYDAY=SU;BYHOUR=21;BYMINUTE=0`（每周日 21:00）、`status=PAUSED`（先不启用）、`cwds=E:\AI\workbuddy\项目-学习1`（周报落盘到该目录 `output/`）。这正是"先写出正确 rrule、但暂不启用"的标准做法。

</details>

---

<details>
<summary><b>L5 · 扩展生态 Connectors / Experts / MCP（1.5 天）</b></summary>

**目标**：把 WorkBuddy 连到外部世界与专业角色——三类扩展机制分别解决"能力不足"和"领域不专业"两类问题。

> **先分清三者**
> - **Connectors（连接器）**：接一个**外部系统/服务**（GitHub、腾讯文档、邮箱、云存储…），让 WorkBuddy 能读写它的数据。
> - **Experts（专家/专家团队）**：加载一个**专业角色**（律师、数据分析师、架构师…），用它的方法论和领域知识帮你干活。
> - **MCP（Model Context Protocol）**：接一个**标准化的工具服务器**（本地或远程），把任意工具/API 变成 WorkBuddy 可调用的能力。

---

### 案例 1 · Connectors：连接 GitHub，自动管理仓库

**场景**：你希望 WorkBuddy 直接读取某仓库的 Issue、列出未合并的 PR、甚至帮你起草提交说明，而不用手动去网页操作。

**步骤**：
1. 在对话里说"帮我接一个能管理 GitHub 仓库的连接器" → WorkBuddy 调 `recommend-connectors` 搜索候选。
2. 从返回的卡片里挑选 GitHub Connector，点安装/授权（OAuth 或填 token）。
3. 授权后在连接器管理页点"信任"启用。
4. 之后直接下指令："汇总一下 photo-map 仓库本周新增的 Issue 并按优先级排序"。

**效果**：WorkBuddy 现在能直接调用 GitHub API，把仓库数据拉进对话、生成报告，无需你来回复制粘贴。

---

### 案例 2 · Experts：请"架构设计专家"评审照片地图方案

**场景**：你的照片地图（Electron + Leaflet + SQLite）在"十万张照片地图渲染"上卡顿，想听听专业意见。

**步骤**：
1. 说"我想找一个能评审前端/全栈架构的专家" → WorkBuddy 调 `recommend-experts` 搜索。
2. 选"架构设计专家"团队并启用（注意：一次只能启用一个 Expert 或专家团队）。
3. 把背景贴给它："Electron 主进程用 SQLite 存 10w 条带经纬度的照片，渲染端用 Leaflet，缩略图用 sharp，现在地图标点多就卡，怎么优化？"
4. 专家会给出索引设计、分块加载、Web Worker 渲染等具体建议。

**效果**：得到一个有方法论支撑、针对你项目的评审，而不是泛泛而谈。

---

### 案例 3 · MCP：配置 Playwright MCP，让 WorkBuddy 会"开网页、点按钮"

**场景**：之前你想"打开 mfsshazure 并点击登录"，但 agent-browser 在沙箱装不上。用 Playwright MCP 即可标准化接入浏览器自动化。

**步骤**：
1. 编辑 `~/.workbuddy/mcp.json`（注意：是 `mcp.json`，**不是** `.mcp.json` 隐藏文件），写入：
   ```json
   {
     "mcpServers": {
       "playwright": {
         "command": "npx",
         "args": ["@playwright/mcp@latest"]
       }
     }
   }
   ```
2. 凭证/配置按提供方文档放 `env` / `headers` / `args`（本例无需）。
3. 打开连接器管理页 → 找到 playwright → 点"信任"启用（写配置不会自动激活）。
4. 之后即可下指令："用浏览器打开 https://mfsshazure.draegercn.com 并点击登录按钮"。

**效果**：WorkBuddy 获得标准化的浏览器自动化能力，登录、填表、截图都能一句话完成。

> **配置口诀**：`command`+`args`+`env` 写进 `mcp.json` → 管理页"信任" → 才能用。别漏掉"信任"这步。

**练习**：亲手走一遍案例 3——把上面的 Playwright MCP 配置写进 `mcp.json`，到连接器管理页点"信任"，然后让 WorkBuddy 打开一个网页并截图，验证整条链路。

</details>

---

<details>
<summary><b>L6 · 多模态生成（1.5 天）</b></summary>

**目标**：文字之外的生产力——图像、视频、3D。

- [ ] 图像：`ImageGen`（文生图 / 图生图）
- [ ] 视频：`VideoGen`（文生视频 / 图生视频）
- [ ] 3D：`3D模型与视频特效` 技能（文生 3D / 图生 3D / 特效模板）
- [ ] 路由原则：按输出类型选工具，不要混用
- [ ] 合规：不涉及政治敏感、未成年人等禁用内容

**练习**：用一张你的照片生成"拥抱/变身"类特效模板，体验图生视频/3D 链路。

</details>

---

<details>
<summary><b>L7 · 可视化 Visualizer（1 天）</b></summary>

**目标**：用内联图表/图示替代大段文字，提升表达力。

- [ ] 何时用：`read_me` 加载模块（`diagram`/`chart`/`mockup`/`interactive`/`art`）
- [ ] `show_widget` 写法：SVG 用 `viewBox="0 0 680 ..."`，HTML 不要包 `<html>/<body>`
- [ ] 教育/讲解类请求**默认就该可视化**
- [ ] 主题匹配：浅色主题用浅底深字，深色反之

**练习**：把 L2 的"任务→工具"对照表，用 `show_widget` 渲染成一张交互式图表。

</details>

---

<details>
<summary><b>L8 · 记忆系统（0.5 天，但贯穿全程）</b></summary>

**目标**：理解三层记忆的读写职责，避免"它为什么忘了/记错了"。

- [ ] 云记忆（只读）：自动注入的用户画像，本地不可改
- [ ] 用户级本地记忆：`~/.workbuddy/MEMORY.md`（跨项目偏好）
- [ ] 项目级记忆：`项目/.workbuddy/memory/`（每日日志 + 长期笔记）
- [ ] 写记忆的时机：完成实质工作后追加当日日志；长期约定写入 MEMORY.md
- [ ] 记忆是补充，不是主交付物

**练习**：在 `项目-学习1/.workbuddy/memory/` 下创建今日日志，记录你今天学到的 3 个要点。

</details>

---

<details>
<summary><b>L9 · 实战项目（3 天，综合）</b></summary>

**目标**：端到端交付一个真实项目，串联上述所有能力。

以你已有的"照片地图"项目为练手场（Electron + Node + Leaflet + SQLite + exiftool）：

- [ ] 用 `Explore` 子代理摸清现有代码结构
- [ ] 用 `TaskCreate` 拆解"自扫描相册 + 自建 SQLite"任务
- [ ] 用 Skills 处理 EXIF 提取、缩略图、地图渲染合规
- [ ] 用 `present_files` 持续交付可预览的产出
- [ ] 把踩坑与解决路径写入项目记忆

**练习**：独立完成一个从"扫描入库 → 地图渲染 → 缩略图预览"的最小可用链路。

</details>

---

<details>
<summary><b>L10 · 进阶交付（1 天）</b></summary>

**目标**：把成果变成可分享/可运行的产物。

- [ ] 发布为应用：静态站 / 小游戏 / Node·Python·Go 服务一键上线分享链接
- [ ] 资料库：在线文档、数据表、看板、网盘协作
- [ ] 离线打包：portable exe 思路（参考你照片地图的最终交付形态）
- [ ] 技能沉淀：把本大纲 L1–L9 的通用套路存成可复用技能

</details>

---

<details open>
<summary><b>📝 自测题库</b></summary>

> 自测题已抽到独立文件 **[`问题集.md`](问题集.md)**，方便你每次练习后单独打开、闭卷作答。
> 规则：每轮题目**只来自你已经练习过的章节**（不出现未学章节）；先不翻资料口述/写下答案，再展开参考答案对照，每题要求"说清为什么"。新增轮次按 问题集.md 里的模板继续追加。

</details>

---

<details>
<summary><b>学习检查清单（里程碑）</b></summary>

| 里程碑 | 标志                | 预计耗时 |
| --- | ----------------- | ---- |
| 入门  | 能独立用文件/命令/搜索完成小任务 | 2 天  |
| 进阶  | 能熟练调用并创建 Skills   | 4 天  |
| 自动化 | 能配置一次性/周期自动化      | 5 天  |
| 多模态 | 能生成图/视频/3D 并合规使用  | 7 天  |
| 实战  | 端到端交付真实项目         | 10 天 |
| 精通  | 能发布应用、沉淀技能、扩展生态   | 12 天 |

</details>

---

<details>
<summary><b>学习纪律（建议）</b></summary>

1. **每个模块都动手**，只"看懂"不算学会。
2. **截图即反馈**：遇到 UI 问题直接截图，WorkBuddy 能即时定位根因。
3. **先找 Skills 再硬刚**：遇到平台/专业任务，先 `find-skills`。
4. **记录踩坑**：把"以为能行但其实不行"的点写进项目记忆。
5. **善用 Agent 子代理**：大范围探索、长任务交给 `Explore`/`general-purpose`，省上下文。

</details>

---

> 本大纲会随你的学习进度持续迭代。每完成一个模块，回来打勾，并在 `项目-学习1/.workbuddy/memory/` 留下当日笔记。
