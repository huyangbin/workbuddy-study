# WorkBuddy 学习 · 自测问题集

> **出题规则**：每练完一个/几个章节，就新增一轮（Round），题目**只来自你已经练习过的章节**，不出现未学章节的内容。
> **作答规则**：先闭卷口述或写下答案，再展开对应参考答案对照；每题要求"说清为什么"，不是背名词。
> 新增轮次时，复制文末「Round 模板」继续往下写即可。

---

## 🟦 Round 1 · L1 + L2（已练习章节）

**1.（L1 运行环境）** WorkBuddy 的 managed Python 和传统的 `virtualenv` 本质区别是什么？要查看它当前装了哪些第三方库，该用什么命令？为什么不能直接用系统里的 `pip list`？

**2.（L1 运行环境）** 在 WorkBuddy 里用 Bash 跑 Python/Node 脚本时，为什么必须用完整绝对路径调用解释器（如 `C:\Users\27598\.workbuddy\binaries\python\versions\3.13.12\python.exe`）？如果直接写 `python script.py` 会怎样？

**3.（L1 文件操作）** 使用 `Edit` 工具修改文件时，哪些情况会导致失败？请举两个。

**4.（L2 工具选择）** 任务是"在项目中找出所有名为 `config.json` 的文件"。该用 `Glob` 还是 `Grep`？为什么？再补一句：若要"找出所有调用了 `readFile()` 的文件和行号"，又该用哪个工具？

**5.（L2 智能体）** 什么场景下应该把搜索/探索任务交给 `Agent(Explore)` 子代理，而不是自己手工 `Grep`？子代理相比主对话最大的好处是什么？

<details>
<summary>🔑 Round 1 参考答案（自测后再展开）</summary>

**1.** managed Python 是装在 WorkBuddy 自有目录（`C:\Users\27598\.workbuddy\binaries\`）里的**完整独立解释器**，与系统 Python 在目录级隔离、互不干扰；传统 `venv` 只是依赖某个 base Python 的轻量副本，只隔离 site-packages。查看库用 `C:\Users\27598\.workbuddy\binaries\python\versions\3.13.12\python.exe -m pip list`；系统 `pip list` 看到的是你机器系统 Python 的库，不是这个隔离运行时里的，所以会"对不上"。

**2.** 环境里同时存在 managed 和系统两套 Python/Node，裸写 `python` 会落到 PATH 里的系统解释器——可能版本不对，也读不到隔离运行时里装的库/脚本依赖。用完整绝对路径能确保调用的是 managed 运行时，行为可预期。

**3.** `Edit` 失败的常见情况（举两个即可）：① `old_string` 与磁盘当前内容**不完全一致**（多/少空格、换行、缩进不符都会失败）；② `old_string` 与 `new_string` **完全相同**（没有实质改动）；③ `old_string` 在文件中**不唯一**且未用 `replace_all`。

**4.** 找"文件在哪/叫什么名"用 `Glob`（按路径/通配符匹配，**不读内容**），所以 `config.json` 用 `Glob`（`**/config.json`）。找"内容里写了什么"用 `Grep`；`readFile()` 的调用处用 `Grep`（可带 `-n` 出行号）。

**5.** 当任务**开放、跨多目录、命名约定不统一、需要综合判断架构或调用关系**时（如"摸清照片地图项目的扫描入库链路"），应交给 `Agent(Explore)`。最大好处：子代理自主多轮搜索、把结果汇总后回传，**不占用主对话的上下文窗口**（省 context）。

</details>

---

## 📋 Round 模板（复制后继续写，题目只来自已练习章节）

```
## 🟦 Round N · （已练习章节：Lx / Ly …）

**1.** （题面，仅限已学章节）
**2.** （题面）
**3.** （题面）
**4.** （题面）
**5.** （题面）

<details>
<summary>🔑 Round N 参考答案（自测后再展开）</summary>

**1.** （答案）
**2.** （答案）
**3.** （答案）
**4.** （答案）
**5.** （答案）

</details>
```
